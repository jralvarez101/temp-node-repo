// optional export instead of using const we go directly to export
module.exports.items = ['item1', 'item2'];

// Other export option is set up a property and make it equal to something

const person = {
  name: 'bob',
};

module.exports.singlePerson = person;
